import 'package:flutter/material.dart';

class ProductAdminManage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Admin Manage Page'),
    );
  }
}
