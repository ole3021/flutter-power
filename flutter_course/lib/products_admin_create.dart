import 'package:flutter/material.dart';

class ProductAdminCreate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Admin Create Page'),
    );
  }
}
